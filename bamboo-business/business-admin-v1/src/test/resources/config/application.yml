spring:
  application:
    name: adminV1
  profiles:
    active: junit
server:
  port: 11003
  servlet:
      context-path: /biz-admin/v1

mybatis:
  mapper-locations: classpath:mapper/*.xml

logging:
  level:
    org.panda.business.admin: debug
