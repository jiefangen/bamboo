spring:
  application:
    name: admin
  profiles:
    active: dev
server:
  port: 10001
  servlet:
      context-path: /biz-admin

mybatis:
  mapper-locations: classpath:mapper/*.xml

logging:
  level:
    org.panda.business.admin: info
