spring:
  application:
    name: business-official
  profiles:
    active: junit
server:
  port: 11002
  servlet:
    context-path: /business-official

mybatis:
  mapper-locations: classpath:mapper/*.xml

logging:
  level:
    org.panda.business.official: debug