spring:
  application:
    name: notice-service
  profiles:
    active: dev
  messages:
    basename: message/error/notice-error,message/info/notice-info
server:
  port: 11002

logging:
  level:
    org.panda.ms.notice: debug
