spring:
  application:
    name: notice-service
  profiles:
    active: junit
server:
  port: 11102
  servlet:
    context-path: /notice-service
    encoding:
      charset: UTF-8
      force: true
      enabled: true

logging:
  level:
    org.panda.service.doc: debug
