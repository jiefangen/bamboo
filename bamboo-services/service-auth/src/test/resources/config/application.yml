spring:
  application:
    name: ms-auth
  profiles:
    active: junit
server:
  port: 11106
  servlet:
      context-path: /auth

mybatis:
  mapper-locations: classpath:mapper/*.xml

logging:
  level:
    org.panda.business.admin: debug
