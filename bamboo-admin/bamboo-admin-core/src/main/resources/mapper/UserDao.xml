<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.panda.modules.system.dao.UserDao">
    <resultMap id="BaseResultMap" type="org.panda.modules.system.domain.User">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="nick_name" property="nickName" jdbcType="VARCHAR"/>
        <result column="phone" property="phone" jdbcType="VARCHAR"/>
        <result column="sex" property="sex" jdbcType="VARCHAR"/>
        <result column="email" property="email" jdbcType="VARCHAR"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="last_password_reset_time" property="lastPasswordResetTime" jdbcType="TIMESTAMP"/>
        <!-- 关联属性 -->

    </resultMap>

    <sql id="Base_Column_List">
        id, username, password, phone, sex, email, create_time, last_password_reset_time
    </sql>

    <select id="findByUsername" parameterType="string" resultMap="BaseResultMap">
        SELECT
           <include refid="Base_Column_List"/>
        FROM USER WHERE 1=1
        <if test="username!=null and username!=''">
            AND  username like  #{username,jdbcType=VARCHAR}
        </if>
    </select>
</mapper>