<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.panda.modules.system.dao.UserDao">
    <resultMap id="BaseUserMap" type="org.panda.modules.system.domain.po.UserPO">
        <id column="user_id" property="id" jdbcType="BIGINT"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="phone" property="phone" jdbcType="VARCHAR"/>
        <result column="nickname" property="nickname" jdbcType="VARCHAR"/>
        <result column="email" property="email" jdbcType="VARCHAR"/>
        <result column="sex" property="sex" jdbcType="VARCHAR"/>
        <result column="disabled" property="disabled" jdbcType="INTEGER"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
        <!-- 关联属性Roles -->
        <!--<collection property="roles" ofType="org.panda.modules.system.domain.po.RolePO">-->
            <!--<id column="role_id" property="id" jdbcType="BIGINT"/>-->
            <!--<result column="role_name" property="roleName" jdbcType="VARCHAR"/>-->
            <!--<result column="description" property="description" jdbcType="VARCHAR"/>-->
        <!--</collection>-->
    </resultMap>

    <select id="findUsers" resultMap="BaseUserMap">
        SELECT
          id user_id, username, password, nickname, phone, sex, disabled, email, create_time, update_time
        FROM SYS_USER
        <trim prefix="WHERE" prefixOverrides="AND |OR ">
            <if test="keyword != null and keyword != ''">
                OR username like  CONCAT('%',#{keyword,jdbcType=VARCHAR},'%')
                OR nickname like  CONCAT('%',#{keyword,jdbcType=VARCHAR},'%')
            </if>
        </trim>
    </select>

    <select id="findByUsername" resultMap="BaseUserMap">
        SELECT
            u.id user_id, u.username, u.password, u.nickname, u.phone, u.sex, u.disabled, u.email, u.create_time, u.update_time
        FROM sys_user u WHERE username = #{username,jdbcType=VARCHAR}
    </select>
</mapper>