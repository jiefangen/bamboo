spring:
  application:
    name: register
server:
  port: 12002

eureka:
  client:
    # 用于指定该客户端实例是否在Eureka服务器上注册自己的信息以提供其他服务进行发现
    register-with-eureka: false
    # 用于指定该客户端是否获取Eureka服务器上的注册信息
    fetch-registry: false
